{
  "name": "pta",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "vp fmt --write",
    "lint": "vp lint",
    "ready": "vp check && vp run -r check && vp run -r build",
    "setup": "vp install && vp config"
  },
  "devDependencies": {
    "vite-plus": "^0.1.24"
  },
  "packageManager": "pnpm@11.5.1"
}
