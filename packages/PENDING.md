- agent 介入的调度是否默认关闭、仅接受显式排期？（暂按「地板自动、红利显式」实现：内建仅零 LLM 地板扫描，agent 任务须显式 cron 条目；Fred 会话已裁方向，钉入与原因待其确认）
- cron 能力是否定位为通用调度、不绑巡检？（暂按通用实现：动作开放 inspect/derive/agent 三类，agent 动作承接投影编译与日报等消费场景；Fred 会话已裁方向，钉入待确认）
- runtime 自写 POSIX cron 五段子集解析器，是否与 core「标准格式交给带跨实现测试套件的标准解析器」的判断相冲突？（暂自写：cron 无官方测试套件也无零依赖库可引；该判断字面只辖 core 域，是否延展至 packages 全域待裁）
- 巡检推导与评估的提示词是否属转换实现、不开放用户配置？（暂硬编码于 cli 实现随版本走；转换实现标识——提示词模板与 agent 命令的哈希——未入推导记录，提示词或模型变化不驱动缓存失效，是否补记待裁）
