- 条目规范化所称“首尾空白”采用哪一字符集合？当前按 ECMAScript `String.trim` 的空白集合处理，再做 NFC 规范化；规范需明确以保证跨语言复算一致。
- `TRUTH.md` frontmatter 的语法子集与错误恢复规则是什么？当前以文档示例所需的顶层 `path`、块状或行内 `files`、块状 `dependsOn` YAML 形态提取，保留原文并对未闭合 frontmatter 暴露问题。
- 仅有 `PENDING.md` 变更的领域应采用哪种触面分类？当前单列为 `inbox-only` 收件箱活动，不并入真相记录或实现触面，也不产生漂移嫌疑。
- 变更分类如何取得待裁决背景而不把文件读取引入纯函数？当前由调用者将按领域提取的待裁决条目作为可选第三参数传入，缺省按没有可用背景呈现。
- `pta.toml` 的完整解析与错误暴露规则是什么？当前仅提取规范示例形态的字符串数组 `externalRoots`，无效根作为不可扫描的事实保留。
- 完整条目标识需要线性序列化时，各部件的边界与转义如何规定？当前只暴露“领域标识 + 文件种类”的容器与内容哈希组成的结构化值，不私设字符串分隔格式。
- 领域发现的工作树范围是否包含未入库、被忽略目录与符号链接目录？当前从给定仓库根扫描普通目录，不跟随符号链接，并排除版本库元数据 `.git` 与常规依赖目录 `node_modules`，以免把仓库树外内容声明为项目领域。
- 列表外内容与未闭合 frontmatter 没有合法条目标识时，核查信号应落到何处？当前落到所属领域声明，使维护者仍可依据 `file:line` 修订。
- 同一结构问题涉及多个落点时应产生一条聚合信号还是逐个可修订落点产生信号？当前按后出现的可修订落点逐条产生，保证每条信号只有一个明确落点。
